apiVersion: kagent.dev/v1alpha2
kind: RemoteMCPServer
metadata:
  name: incident-mcp-server
  namespace: kagent
spec:
  description: "Incident MCP server — open incident tracking, alert history, runbooks, incident updates, and Slack notifications"
  url: http://incident-mcp-server.services-dev.svc.cluster.local:3008/mcp
  protocol: STREAMABLE_HTTP
  timeout: 30s
