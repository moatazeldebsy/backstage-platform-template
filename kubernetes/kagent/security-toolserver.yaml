apiVersion: kagent.dev/v1alpha2
kind: RemoteMCPServer
metadata:
  name: security-mcp-server
  namespace: kagent
spec:
  description: "Security MCP server — Dependabot alerts, secret-rotation status, and Kyverno policy violations"
  url: http://security-mcp-server.services-dev.svc.cluster.local:3010/mcp
  protocol: STREAMABLE_HTTP
  timeout: 30s
