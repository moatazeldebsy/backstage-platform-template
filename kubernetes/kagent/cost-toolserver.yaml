apiVersion: kagent.dev/v1alpha2
kind: RemoteMCPServer
metadata:
  name: cost-mcp-server
  namespace: kagent
spec:
  description: "Cost MCP server — namespace costs, team spend, budget overruns, rightsizing, and EOM forecasting"
  url: http://cost-mcp-server.services-dev.svc.cluster.local:3007/mcp
  protocol: STREAMABLE_HTTP
  timeout: 30s
