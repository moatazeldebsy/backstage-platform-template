apiVersion: kagent.dev/v1alpha2
kind: RemoteMCPServer
metadata:
  name: contract-mcp-server
  namespace: kagent
spec:
  description: "Contract MCP server — register OpenAPI specs, generate Pact tests, detect breaking changes, validate consumer/provider compatibility"
  url: http://contract-mcp-server.services-dev.svc.cluster.local:3003/mcp
  protocol: STREAMABLE_HTTP
  timeout: 60s
