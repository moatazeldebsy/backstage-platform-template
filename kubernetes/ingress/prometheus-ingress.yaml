# Production ALB Ingress for Prometheus (kube-prometheus-stack release name: prometheus)
# Apply after kube-prometheus-stack is deployed to EKS:
#   kubectl apply -f kubernetes/ingress/prometheus-ingress.yaml
#
# hosts: [] — matches any hostname; set to real domain when Route53/DNS is configured.
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: prometheus
  namespace: monitoring
  annotations:
    kubernetes.io/ingress.class: alb
    alb.ingress.kubernetes.io/scheme: internet-facing
    alb.ingress.kubernetes.io/target-type: ip
    alb.ingress.kubernetes.io/backend-protocol: HTTP
spec:
  ingressClassName: alb
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: prometheus-kube-prometheus-prometheus
                port:
                  number: 9090
