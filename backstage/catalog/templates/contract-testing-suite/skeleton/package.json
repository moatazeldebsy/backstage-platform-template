{
  "name": "${{ values.name }}",
  "version": "0.1.0",
  "description": "${{ values.description }}",
  "private": true,
  "scripts": {
    "test": "jest --testPathPattern=pact.spec",
    "verify": "jest --testPathPattern=provider",
    "register-contract": "node scripts/register-contract.mjs"
  },
  "devDependencies": {
    "@pact-foundation/pact": "^16.5.0",
    "@types/jest": "^29.0.0",
    "@types/node": "^20.0.0",
    "jest": "^29.0.0",
    "ts-jest": "^29.0.0",
    "typescript": "^5.4.0"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node"
  },
  "overrides": {
    "qs": "^6.16.0"
  }
}
