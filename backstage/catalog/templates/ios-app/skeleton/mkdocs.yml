site_name: ${{ values.name }}
site_description: ${{ values.description }}
site_author: ${{ values.owner }}
repo_url: https://github.com/${{ values.githubOrg }}/${{ values.repoName }}
repo_name: ${{ values.githubOrg }}/${{ values.repoName }}

docs_dir: docs

nav:
  - Home: index.md

plugins:
  - techdocs-core
