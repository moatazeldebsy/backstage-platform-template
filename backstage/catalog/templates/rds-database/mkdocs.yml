site_name: Managed Postgres (RDS)
site_description: >-
  Spin up an Amazon RDS Postgres instance with multi-AZ failover, automated backups, and IAM auth. Credentials are written to the requesting service namespace as a secret.
docs_dir: docs
nav:
  - Overview: index.md
plugins:
  - techdocs-core
