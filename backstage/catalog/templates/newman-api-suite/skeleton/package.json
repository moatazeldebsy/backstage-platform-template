{
  "name": "${{ values.name }}",
  "version": "0.1.0",
  "description": "${{ values.description }}",
  "private": true,
  "scripts": {
    "test": "newman run collections/${{ values.name }}.postman_collection.json -e environments/dev.postman_environment.json --reporters cli,junit --reporter-junit-export reports/results.xml",
    "report": "newman run collections/${{ values.name }}.postman_collection.json -e environments/dev.postman_environment.json --reporters htmlextra --reporter-htmlextra-export reports/report.html && open reports/report.html"
  },
  "devDependencies": {
    "newman": "^6.2.2",
    "newman-reporter-htmlextra": "^1.23.1"
  },
  "overrides": {
    "flatted": "^3.4.4",
    "handlebars": "^4.7.9",
    "jose": "^4.15.9",
    "lodash": "^4.18.1",
    "node-forge": "^1.4.0",
    "qs": "^6.15.3",
    "underscore": "^1.13.8",
    "uuid": "^11.1.1"
  }
}
