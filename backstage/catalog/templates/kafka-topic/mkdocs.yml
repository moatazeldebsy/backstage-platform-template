site_name: Kafka Topic
site_description: >-
  Create a Kafka topic on the shared MSK cluster with partitions, retention, ACLs for the owning service, and an optional schema registry subject.
docs_dir: docs
nav:
  - Overview: index.md
plugins:
  - techdocs-core
