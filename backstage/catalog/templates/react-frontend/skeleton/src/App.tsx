function App() {
  return (
    <main style={{ fontFamily: 'sans-serif', padding: '2rem' }}>
      <h1>${{ values.name }}</h1>
      <p>${{ values.description }}</p>
    </main>
  )
}

export default App
