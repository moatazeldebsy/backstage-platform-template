apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: ${{ values.name }}
  description: ${{ values.description }}
  annotations:
    github.com/project-slug: ${{ values.githubOrg }}/${{ values.repoName }}
    backstage.io/techdocs-ref: dir:.
    backstage.io/kubernetes-id: ${{ values.name }}
    backstage.io/kubernetes-namespace: services-dev
    backstage.io/adr-location: https://github.com/${{ values.githubOrg }}/${{ values.repoName }}/tree/main/docs/adr
    backstage.io/runbook-url: https://github.com/${{ values.githubOrg }}/${{ values.repoName }}/blob/main/docs/runbooks/service.md
    cost-center: "${{ values.costCenter }}"
    idp.io/quality-gates: "coverage,static-analysis,vuln-scan"
    grafana/alert-label-selector: "service=${{ values.name }}"
    # Drives the Langfuse tab on this entity's page. Must equal the
    # OTEL_SERVICE_NAME set in helm-values-*.yaml — that value is the Langfuse
    # trace tag the tab filters on.
    langfuse.com/service-name: "${{ values.name }}"
  links:
    # Absolute, like the Grafana and ArgoCD links below. A relative URL such as
    # /langfuse fails catalog validation outright ("expected a string that is a
    # valid url"), which fails the whole catalog:register step.
    - title: Langfuse (AI Observability)
      url: http://langfuse.idp.local
      icon: dashboard
    - title: Grafana Dashboard
      url: http://grafana.idp.local/d/idp-services/idp-services-overview
      icon: dashboard
    - title: ArgoCD App
      url: http://argocd.idp.local/applications/${{ values.name }}-dev
      icon: web
    - title: Runbook
      url: https://github.com/${{ values.githubOrg }}/${{ values.repoName }}/blob/main/docs/runbooks/service.md
      icon: help
spec:
  type: service
  lifecycle: development
  owner: ${{ values.owner }}
  system: internal-developer-platform
