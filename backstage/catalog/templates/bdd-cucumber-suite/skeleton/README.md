# ${{ values.name }}

${{ values.description }}

Cucumber.js BDD suite for `${{ values.targetService }}`.

## Quick start

```bash
npm install
npm test
```

## Adding scenarios

1. Write a `.feature` file in `features/`
2. Implement the step definitions in `steps/`
3. Run `npm test`
