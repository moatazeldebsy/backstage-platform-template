{% if values.platform === 'flutter' %}
name: ${{ values.name | replace("-", "_") }}
description: ${{ values.description }}
version: 0.1.0

environment:
  sdk: '>=3.5.0 <4.0.0'
  flutter: '>=3.24.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  # Uncomment if this package ships assets
  # assets:
  #   - assets/
{% endif %}
