apiVersion: backstage.io/v1alpha1
kind: Resource
metadata:
  name: llm-gateway
  description: >
    LLM provider infrastructure — local Ollama instance (Kind) or OpenAI API (production).
    All AI agent services declare a dependsOn relationship to this resource.
  annotations:
    cost-center: eng-ai
spec:
  type: llm-provider
  lifecycle: production
  owner: ml-team
  system: internal-developer-platform
