replicaCount: 1

image:
  repository: ghcr.io/moatazeldebsy/python-demo0
  tag: "latest"
  pullPolicy: IfNotPresent

service:
  port: 80
  targetPort: 

ingress:
  enabled: true
  className: "nginx"
  hosts:
    - host: python-demo0-dev.idp.local
      paths:
        - path: /
          pathType: Prefix

resources:
  requests:
    cpu: 25m
    memory: 32Mi
  limits:
    cpu: 100m
    memory: 64Mi

env:
  - name: LOG_LEVEL
    value: debug
  - name: ENVIRONMENT
    value: dev

podLabels:
  team: "qa-team"
  cost-center: ""
  environment: dev
  app.kubernetes.io/part-of: internal-developer-platform
