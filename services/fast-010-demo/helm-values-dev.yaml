replicaCount: 1

image:
  repository: ghcr.io/moatazeldebsy/fast-010-demo
  tag: "latest"
  pullPolicy: IfNotPresent

service:
  port: 80
  targetPort: 8000

ingress:
  enabled: true
  className: "nginx"
  hosts:
    - host: fast-010-demo-dev.idp.local
      paths:
        - path: /
          pathType: Prefix

resources:
  requests:
    cpu: 25m
    memory: 32Mi
  limits:
    cpu: 100m
    memory: 64Mi

env:
  - name: LOG_LEVEL
    value: debug
  - name: ENVIRONMENT
    value: dev

podLabels:
  team: "platform-team"
  cost-center: "eng-platform"
  environment: dev
  app.kubernetes.io/part-of: internal-developer-platform
